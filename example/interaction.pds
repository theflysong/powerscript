# c_interaction.c
#
# #include <stdio.h>
# 
# int main(int argc, const char **argv) {
#     int a, b;
#     scanf("%d%d", &a, &b);
#     printf("%d", a + b);
#     return 0;  
# }

let result = 0;

interaction(c_interaction()) {
    printf("2 3\n");
    scanf("%d", result);
}

echo(result);